# Railway/Nixpacks Configuration
# This tells Railway to install Node.js, Python, yt-dlp, and ffmpeg

[phases.setup]
nixPkgs = ["nodejs_20", "python310", "python310Packages.pip", "ffmpeg", "yt-dlp"]

[start]
cmd = "npm run start"

